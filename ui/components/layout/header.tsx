// Layout top-bar replaced by Sidebar footer.
// User info and logout now live in the sidebar.
// File retained for export compatibility.
export function Header(_props: { user: { email: string; name: string } }) {
  return null;
}
